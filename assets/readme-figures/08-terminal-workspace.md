---
figure: 08-terminal-workspace.png
mode: readme
anchor: 02-boundary-clamp.png
character: 01-packet-bot-sheet.png
language: en
---

**QA:** Final PNG inspected in color and grayscale; glyphs and line styles retain meaning without hue. README embedding checked locally at desktop and mobile widths; enlarge images for small-screen labels.

**Caption:** Conceptual Herdr workspace: the orchestrator owns the run; implementer and verifier get named tabs. Selected worker tab: 75% agent, 25% lifecycle monitor. Work via the CLI from the orchestrator tab; monitor review is not coordinator acceptance.

**Source:** Original conceptual terminal layout, not a screenshot. Based on herdr-axi worker tab labels and agentRatio default at 6688fee. No user screenshot or live project data supplied to generation.

**Deviations from anchor:** A single large terminal workspace instead of source-boundary-sink; a tab rail and 75/25 stacked panes are the central mechanism; Packet Bot operates a tab selector at the upper-right margin.

**Three-second read:** Use the orchestrator tab to delegate; each named worker tab keeps work above and its monitor below.

**Prompt:**

```text
Generate one standalone 16:9 landscape technical fieldnote figure, 2048x1152 if available, intended for an English GitHub README. Use case: infographic-diagram. NEW composition, not an edit of the attached references.
Visual DNA: white background, thin black analyst-notebook sketch strokes with subtle natural wobble, restrained technical line art, abundant whitespace (at least 30%), legible large handwritten English labels. Flat diagram, no decorative heading, no gradient, no dense text, no corporate cards, no shields, locks, people or cyber imagery.
Readme character: exactly ONE small Packet Bot matching reference 1: horizontal rounded packet body, tiny header ticks, checksum corner notch, minimal screen face, modular connector tool arms, no hands or legs. It performs the specified verification/control action. All system boxes are faceless, without header ticks or checksum notches. Reference 2 is the locked quality/composition anchor (02-boundary-clamp), not a layout template. Any further image is a previously accepted figure for set consistency only.
Encoding: ordinary relations thin black arrows. Risk is a red solid arrow. Controls are BLUE GATE/CLAMP/LATCH GLYPHS directly on a path, never a blue connection or blue alternative route. Ownership/trust boundary is AMBER DASHED. Residual caveat AMBER DOTTED with an open gap, never solid. Verified pass uses a small GREEN CHECK TAG, only after a real control. Meaning must survive grayscale. No distinction by color alone.
Constraints: one mechanism only, 3–7 short labels, each 1–4 words; render ONLY the requested labels verbatim, no other copy or tiny pseudo-text. Keep all labels readable when displayed 800px wide. Every mark must explain a relationship. Keep corner and edge margins generous. Do not copy either reference's layout. No system-as-mascot. No claim of full sandbox security or autonomous billing changes.

Core three-second read: Use the orchestrator tab to delegate; each named worker tab keeps work above and its monitor below.
Mechanism: navigating an owned Herdr terminal workspace. Structure: nested rooms, a conceptual product illustration, NOT a screenshot and NOT using any user's screenshot or project data. One generous hand-drawn light terminal window spans nearly the entire canvas width, framed by whitespace. Use a narrow left workspace rail (roughly 12% of window), a top horizontal tab strip over the remaining 88%, and ONE selected worker tab's content below. In the rail one selected generic workspace labelled "demo". Across the top exactly three tabs: "orchestrator", "parser · copilot", "verify · claude". Middle parser tab selected using a modest blue clip on its top edge. Both other tabs remain clearly visible. The single small Packet Bot sits OUTSIDE upper-right window border, operating that tab-selection clip with a modular probe arm; no character inside any terminal pane. Main selected-tab area splits HORIZONTALLY: upper worker pane is exactly THREE TIMES the height of the lower monitor pane. Upper pane contains two short widely spaced monospaced result lines: "files: parser.mjs" and "checks: passed", plus only a few thin black short text strokes that do not imitate any long actual command. Lower monitor pane contains ONE monospaced line "task: review", with a tiny BLUE receipt validation gate glyph to its left, no green accepted check. This is review pending, not an accepted result. No right sidebar, no floating menus, no split showing multiple selected tabs, no private project names, no logos, no giant text walls, no macOS chrome. At least 30% of full figure remains open white space (including empty terminal interiors). Terminal text must be crisp, dark, comfortably legible. Labels ONLY seven as quoted: "demo", "orchestrator", "parser · copilot", "verify · claude", "files: parser.mjs", "checks: passed", "task: review". Do not add a title or small annotations. Exact 75%/25% geometry is important; not equal halves. This light conceptual recreation should explain how a human navigates orchestrator and worker tabs at a glance.
Anchor invariants: thin sketch line, quiet white canvas, short labels, one consistent functional Packet Bot. Mandatory deviations: A single large terminal workspace instead of source-boundary-sink; a tab rail and 75/25 stacked panes are the central mechanism; Packet Bot operates a tab selector at the upper-right margin.
```
